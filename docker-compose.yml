services:
  python-dev:
    image: python:3.14
    container_name: python-dev
    volumes:
      - ./:/app
    working_dir: /app
    stdin_open: true   # equivalente ao -i
    tty: true           # equivalente ao -t